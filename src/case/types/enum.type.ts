export enum Status {
    pending = "pending",
    open = "",
    inProgress = "inProgress",
    closed = "closed",
    rejected = "rejected"
}

export enum Priority {
    low = "low",
    medium = "medium",
    high = "high"
}